services:
  barni-assistant:
    build: .
    container_name: barni-assistant
    ports:
      - "80:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    env_file:
      - .env
    restart: always

volumes:
  data:
  logs:
